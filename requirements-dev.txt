# DCS Natural Language ATC - Development/Testing Dependencies
# For CI/CD and development without audio hardware

# Core AI/ML
ollama>=0.1.0
numpy>=1.24.0

# Audio Processing (mock-friendly, no hardware required)
# Note: pyaudio and sounddevice are excluded for CI/CD
# They will be mocked in tests
pydub>=0.25.1
scipy>=1.10.0

# Speech Recognition (will be mocked in tests)
# faster-whisper excluded for CI/CD (requires CUDA/CPU optimization)

# NLP
# spacy excluded for CI/CD (large download, will mock in tests)

# Networking
requests>=2.31.0
websockets>=12.0

# Configuration
pyyaml>=6.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# Development
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
